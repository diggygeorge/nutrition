# Menu items are updated each day at 4:44 UTC.
name: Update Food

on:
  schedule:
    - cron: '44 4 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    name: Web Scrape
    steps:
    - uses: actions/checkout@v4
    # Clones repository
    - run: |
        cd src
        python scrape.py